/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/

var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __markAsModule = (target) => __defProp(target, "__esModule", { value: true });
var __export = (target, all) => {
  __markAsModule(target);
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __reExport = (target, module2, desc) => {
  if (module2 && typeof module2 === "object" || typeof module2 === "function") {
    for (let key of __getOwnPropNames(module2))
      if (!__hasOwnProp.call(target, key) && key !== "default")
        __defProp(target, key, { get: () => module2[key], enumerable: !(desc = __getOwnPropDesc(module2, key)) || desc.enumerable });
  }
  return target;
};
var __toModule = (module2) => {
  return __reExport(__markAsModule(__defProp(module2 != null ? __create(__getProtoOf(module2)) : {}, "default", module2 && module2.__esModule && "default" in module2 ? { get: () => module2.default, enumerable: true } : { value: module2, enumerable: true })), module2);
};
var __async = (__this, __arguments, generator) => {
  return new Promise((resolve, reject) => {
    var fulfilled = (value) => {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    };
    var rejected = (value) => {
      try {
        step(generator.throw(value));
      } catch (e) {
        reject(e);
      }
    };
    var step = (x) => x.done ? resolve(x.value) : Promise.resolve(x.value).then(fulfilled, rejected);
    step((generator = generator.apply(__this, __arguments)).next());
  });
};

// src/main.ts
__export(exports, {
  default: () => VaultCleanerPlugin
});
var import_obsidian7 = __toModule(require("obsidian"));

// src/settings.ts
var import_obsidian = __toModule(require("obsidian"));

// src/locales/zh-CN.ts
var zhCN = {
  plugin: {
    name: "Nuke Orphans \u6E05\u7406\u5668",
    openDashboard: "\u6253\u5F00\u63A7\u5236\u9762\u677F",
    cleanAttachments: "\u6E05\u7406\u5B64\u7ACB\u9644\u4EF6",
    cleanNotes: "\u6E05\u7406\u5B64\u7ACB\u7B14\u8BB0",
    cleanAll: "\u6E05\u7406\u6240\u6709\u5B64\u7ACB\u6587\u4EF6",
    gatheringAttachments: "\u6B63\u5728\u6536\u96C6\u5B64\u7ACB\u9644\u4EF6...",
    gatheringNotes: "\u6B63\u5728\u6536\u96C6\u5B64\u7ACB\u7B14\u8BB0...",
    gatheringFiles: "\u6B63\u5728\u6536\u96C6\u5B64\u7ACB\u6587\u4EF6...",
    noOrphanAttachments: "\u672A\u627E\u5230\u5B64\u7ACB\u9644\u4EF6",
    noOrphanNotes: "\u672A\u627E\u5230\u5B64\u7ACB\u7B14\u8BB0",
    noOrphans: "\u672A\u627E\u5230\u5B64\u7ACB\u6587\u4EF6"
  },
  dashboard: {
    title: "\u{1F9F9} Nuke Orphans \u6E05\u7406\u5668",
    scanStatus: "\u{1F4CA} Vault \u626B\u63CF\u72B6\u6001",
    scanning: "\u23F3 \u6B63\u5728\u626B\u63CF...",
    scanComplete: "\u2705 \u626B\u63CF\u5B8C\u6210",
    notScanned: "\u26A0\uFE0F \u5C1A\u672A\u626B\u63CF",
    stats: "\u{1F4C8} \u5B64\u7ACB\u6587\u4EF6\u7EDF\u8BA1",
    orphanAttachments: "\u{1F4CE} \u5B64\u7ACB\u9644\u4EF6",
    orphanNotes: "\u{1F4DD} \u5B64\u7ACB\u7B14\u8BB0",
    totalOrphans: "\u{1F4C1} \u603B\u8BA1",
    actions: "\u{1F50D} \u64CD\u4F5C\u4E2D\u5FC3",
    scanVault: "\u{1F50E} \u626B\u63CF Vault",
    cleanAttachmentsBtn: "\u{1F9F9} \u6E05\u7406\u5B64\u7ACB\u9644\u4EF6",
    cleanNotesBtn: "\u{1F9F9} \u6E05\u7406\u5B64\u7ACB\u7B14\u8BB0",
    cleanAllBtn: "\u{1F4A5} \u6E05\u7406\u6240\u6709\u5B64\u7ACB\u6587\u4EF6",
    autoCleanStatus: "\u2699\uFE0F \u81EA\u52A8\u6E05\u7406\u72B6\u6001",
    autoCleanEnabled: "\u2611 \u81EA\u52A8\u6E05\u7406\uFF1A\u5DF2\u542F\u7528",
    autoCleanDisabled: "\u2610 \u81EA\u52A8\u6E05\u7406\uFF1A\u5DF2\u7981\u7528",
    everyDays: "\u23F1 \u6BCF {days} \u5929\u6267\u884C\u4E00\u6B21",
    deleteStrategy: "\u{1F5D1} \u5220\u9664\u7B56\u7565",
    strategyCustom: "\u79FB\u52A8\u5230\u81EA\u5B9A\u4E49\u5783\u573E\u6876",
    strategyPermanent: "\u6C38\u4E45\u5220\u9664",
    safetyNotice: "\u26A0\uFE0F \u5B89\u5168\u63D0\u793A",
    safetyProtected: "- \u6700\u8FD1 {days} \u5929\u4FEE\u6539\u7684\u6587\u4EF6\u53D7\u4FDD\u62A4",
    safetyPreview: "- \u6240\u6709\u64CD\u4F5C\u9700\u8981\u9884\u89C8\u786E\u8BA4",
    safetyAutoClean: "- \u81EA\u52A8\u6E05\u7406\u4EC5\u5F71\u54CD\u9644\u4EF6\uFF0C\u4E0D\u5F71\u54CD\u7B14\u8BB0",
    scanFirst: "\u8BF7\u5148\u626B\u63CF Vault",
    scanCompleteNotice: "\u626B\u63CF\u5B8C\u6210\uFF1A\u627E\u5230 {count} \u4E2A\u5B64\u7ACB\u6587\u4EF6",
    scanFailed: "\u626B\u63CF\u5931\u8D25\uFF1A{error}"
  },
  preview: {
    title: "\u9884\u89C8\uFF1A{count} \u4E2A\u5F85\u6E05\u7406\u6587\u4EF6",
    fileName: "\u6587\u4EF6\u540D",
    filePath: "\u8DEF\u5F84",
    fileSize: "\u5927\u5C0F",
    fileType: "\u7C7B\u578B",
    fileStatus: "\u72B6\u6001",
    unreferenced: "\u672A\u88AB\u5F15\u7528",
    warning: "\u26A0\uFE0F \u8BF7\u5728\u7EE7\u7EED\u524D\u68C0\u67E5\u4EE5\u4E0A\u6587\u4EF6\u3002",
    confirmCheckbox: "\u6211\u5DF2\u786E\u8BA4\u8FD9\u4E9B\u6587\u4EF6\u53EF\u4EE5\u5220\u9664",
    cancel: "\u274C \u53D6\u6D88",
    execute: "\u{1F9F9} \u6267\u884C\u6E05\u7406"
  },
  settings: {
    title: "\u{1F9F9} Vault Cleaner \u8BBE\u7F6E",
    cleanupSettings: "\u{1F5D1} \u6E05\u7406\u8BBE\u7F6E",
    deleteStrategy: "\u5220\u9664\u7B56\u7565",
    deleteStrategyDesc: "\u6587\u4EF6\u5220\u9664\u65B9\u5F0F",
    strategyCustom: "\u79FB\u52A8\u5230\u81EA\u5B9A\u4E49\u5783\u573E\u6876",
    strategyPermanent: "\u6C38\u4E45\u5220\u9664\uFF08\u5371\u9669\uFF09",
    customTrashFolder: "\u81EA\u5B9A\u4E49\u5783\u573E\u6876\u8DEF\u5F84",
    customTrashFolderDesc: "\u81EA\u5B9A\u4E49\u5783\u573E\u6876\u7684\u6587\u4EF6\u5939\u540D\u79F0",
    protectedDays: "\u6700\u8FD1\u4FEE\u6539\u4FDD\u62A4",
    protectedDaysDesc: "\u5728\u6B64\u5929\u6570\u5185\u4FEE\u6539\u7684\u6587\u4EF6\u4E0D\u4F1A\u88AB\u5220\u9664",
    whitelistFolders: "\u767D\u540D\u5355\u6587\u4EF6\u5939",
    whitelistFoldersDesc: "\u6392\u9664\u626B\u63CF\u7684\u6587\u4EF6\u5939\uFF08\u6BCF\u884C\u4E00\u4E2A\uFF09",
    autoCleanSettings: "\u{1F916} \u81EA\u52A8\u6E05\u7406\u8BBE\u7F6E\uFF08\u4EC5\u9644\u4EF6\uFF09",
    enableAutoClean: "\u542F\u7528\u81EA\u52A8\u6E05\u7406",
    enableAutoCleanDesc: "\u6309\u8BA1\u5212\u81EA\u52A8\u6E05\u7406\u5B64\u7ACB\u9644\u4EF6",
    cleanInterval: "\u6E05\u7406\u95F4\u9694",
    cleanIntervalDesc: "\u81EA\u52A8\u6E05\u7406\u6267\u884C\u9891\u7387\uFF08\u5929\uFF09",
    checkOnVaultOpen: "\u6253\u5F00 Vault \u65F6\u68C0\u67E5",
    checkOnVaultOpenDesc: "\u6253\u5F00 Vault \u65F6\u8FD0\u884C\u81EA\u52A8\u6E05\u7406\u68C0\u67E5",
    checkOnPluginLoad: "\u52A0\u8F7D\u63D2\u4EF6\u65F6\u68C0\u67E5",
    checkOnPluginLoadDesc: "\u52A0\u8F7D\u63D2\u4EF6\u65F6\u8FD0\u884C\u81EA\u52A8\u6E05\u7406\u68C0\u67E5",
    advancedSettings: "\u2699\uFE0F \u9AD8\u7EA7\u8BBE\u7F6E",
    attachmentFolder: "\u8986\u76D6\u9644\u4EF6\u6587\u4EF6\u5939",
    attachmentFolderDesc: "\u9644\u4EF6\u5B58\u50A8\u4F4D\u7F6E\uFF08\u6BCF\u884C\u4E00\u4E2A\uFF09",
    ignorePatterns: "\u5FFD\u7565\u6A21\u5F0F",
    ignorePatternsDesc: "\u6DFB\u52A0\u6B63\u5219\u8868\u8FBE\u5F0F\u6A21\u5F0F\u4EE5\u5728\u641C\u7D22\u5B64\u7ACB\u6587\u4EF6\u65F6\u5FFD\u7565",
    testSettings: "\u6D4B\u8BD5\u8BBE\u7F6E",
    testSettingsDesc: "\u5982\u679C\u8DEF\u5F84\u88AB\u5FFD\u7565\u5219\u663E\u793A\u7EA2\u8272\uFF0C\u5426\u5219\u663E\u793A\u7EFF\u8272",
    alternativeAlg: "\u66FF\u4EE3\u9644\u4EF6\u67E5\u627E\u7B97\u6CD5",
    alternativeAlgDesc: "\u5982\u679C\u5728\u5B50\u6587\u4EF6\u5939\u4E2D\u627E\u4E0D\u5230\u9644\u4EF6\uFF0C\u8BF7\u5C1D\u8BD5\u542F\u7528\u6B64\u9009\u9879"
  },
  action: {
    noFilesToDelete: "\u6CA1\u6709\u8981\u5220\u9664\u7684\u6587\u4EF6",
    deleteSuccess: "\u6210\u529F\u5220\u9664 {count} \u4E2A\u6587\u4EF6",
    deleteFailed: "\u5220\u9664\u6587\u4EF6\u5931\u8D25\uFF1A{error}",
    cannotUndo: "\u65E0\u6CD5\u64A4\u9500\u6C38\u4E45\u5220\u9664",
    restoreSuccess: "\u5DF2\u6062\u590D {count} \u4E2A\u6587\u4EF6",
    restoreFailed: "\u6062\u590D\u6587\u4EF6\u5931\u8D25",
    undoNotSupported: "\u4E0D\u652F\u6301\u64A4\u9500\u6B64\u64CD\u4F5C",
    autoCleanNotice: "\u81EA\u52A8\u6E05\u7406\uFF1A\u5220\u9664\u4E86 {count} \u4E2A\u5B64\u7ACB\u9644\u4EF6",
    autoCleanFailed: "\u81EA\u52A8\u6E05\u7406\u5931\u8D25\uFF1A{error}"
  }
};

// src/locales/en-US.ts
var enUS = {
  plugin: {
    name: "Nuke Orphans Cleaner",
    openDashboard: "Open Dashboard",
    cleanAttachments: "Trash orphaned attachments",
    cleanNotes: "Trash orphaned notes",
    cleanAll: "Trash orphaned files",
    gatheringAttachments: "Gathering orphaned attachments..",
    gatheringNotes: "Gathering orphaned notes..",
    gatheringFiles: "Gathering orphaned files..",
    noOrphanAttachments: "No orphaned attachments have been found",
    noOrphanNotes: "No orphaned notes have been found",
    noOrphans: "No orphaned files have been found"
  },
  dashboard: {
    title: "\u{1F9F9} Nuke Orphans Cleaner",
    scanStatus: "\u{1F4CA} Vault Scan Status",
    scanning: "\u23F3 Scanning vault...",
    scanComplete: "\u2705 Scan complete",
    notScanned: "\u26A0\uFE0F Not scanned yet",
    stats: "\u{1F4C8} Orphan Statistics",
    orphanAttachments: "\u{1F4CE} Orphan Attachments",
    orphanNotes: "\u{1F4DD} Orphan Notes",
    totalOrphans: "\u{1F4C1} Total Orphans",
    actions: "\u{1F50D} Action Center",
    scanVault: "\u{1F50E} Scan Vault",
    cleanAttachmentsBtn: "\u{1F9F9} Clean Orphan Attachments",
    cleanNotesBtn: "\u{1F9F9} Clean Orphan Notes",
    cleanAllBtn: "\u{1F4A5} Clean All Orphans",
    autoCleanStatus: "\u2699\uFE0F Auto Clean Status",
    autoCleanEnabled: "\u2611 Auto Clean: Enabled",
    autoCleanDisabled: "\u2610 Auto Clean: Disabled",
    everyDays: "\u23F1 Every {days} days",
    deleteStrategy: "\u{1F5D1} Delete Strategy",
    strategyCustom: "Move to custom trash",
    strategyPermanent: "Permanent Delete",
    safetyNotice: "\u26A0\uFE0F Safety Notice",
    safetyProtected: "- Files modified in the last {days} days are protected",
    safetyPreview: "- All operations require preview confirmation",
    safetyAutoClean: "- Auto clean only affects attachments, not notes",
    scanFirst: "Please scan vault first",
    scanCompleteNotice: "Scan complete: {count} orphans found",
    scanFailed: "Scan failed: {error}"
  },
  preview: {
    title: "Preview: {count} files to clean",
    fileName: "Name",
    filePath: "Path",
    fileSize: "Size",
    fileType: "Type",
    fileStatus: "Status",
    unreferenced: "Unreferenced",
    warning: "\u26A0\uFE0F Please review the files above before proceeding.",
    confirmCheckbox: "I have confirmed these files can be deleted",
    cancel: "\u274C Cancel",
    execute: "\u{1F9F9} Execute Cleanup"
  },
  settings: {
    title: "\u{1F9F9} Vault Cleaner Settings",
    cleanupSettings: "\u{1F5D1} Cleanup Settings",
    deleteStrategy: "Delete Strategy",
    deleteStrategyDesc: "How files should be deleted",
    strategyCustom: "Move to custom trash",
    strategyPermanent: "Permanent Delete (Dangerous)",
    customTrashFolder: "Custom Trash Folder",
    customTrashFolderDesc: "Name of the custom trash folder",
    protectedDays: "Recently Modified Protection",
    protectedDaysDesc: "Files modified within this many days will not be deleted",
    whitelistFolders: "Whitelist Folders",
    whitelistFoldersDesc: "Folders to exclude from scanning (one per line)",
    autoCleanSettings: "\u{1F916} Auto Clean Settings (Attachments Only)",
    enableAutoClean: "Enable Auto Clean",
    enableAutoCleanDesc: "Automatically clean orphaned attachments on a schedule",
    cleanInterval: "Clean Interval",
    cleanIntervalDesc: "How often to run auto clean (in days)",
    checkOnVaultOpen: "Check on Vault Open",
    checkOnVaultOpenDesc: "Run auto clean check when vault is opened",
    checkOnPluginLoad: "Check on Plugin Load",
    checkOnPluginLoadDesc: "Run auto clean check when plugin is loaded",
    advancedSettings: "\u2699\uFE0F Advanced Settings",
    attachmentFolder: "Override Attachment Folder",
    attachmentFolderDesc: "Where attachments are stored (one per line)",
    ignorePatterns: "Ignore Patterns",
    ignorePatternsDesc: "Add regex patterns to ignore when searching for orphans",
    testSettings: "Test Settings",
    testSettingsDesc: "If the path is ignored it will be red, otherwise green",
    alternativeAlg: "Alternative Attachments Finding Algorithm",
    alternativeAlgDesc: "Try enabling this if attachments are not found in subfolders"
  },
  action: {
    noFilesToDelete: "No files to delete",
    deleteSuccess: "Successfully deleted {count} files",
    deleteFailed: "Failed to delete files: {error}",
    cannotUndo: "Cannot undo permanent deletion",
    restoreSuccess: "Restored {count} files",
    restoreFailed: "Failed to restore files",
    undoNotSupported: "Undo not supported for this operation",
    autoCleanNotice: "Auto clean: removed {count} orphaned attachments",
    autoCleanFailed: "Auto clean failed: {error}"
  }
};

// src/locales/index.ts
var translations = {
  "zh-CN": zhCN,
  "en-US": enUS
};
var currentLanguage = "en-US";
function setLanguage(lang) {
  currentLanguage = lang;
}
function t() {
  return translations[currentLanguage];
}
function getAvailableLanguages() {
  return [
    { code: "en-US", name: "English" },
    { code: "zh-CN", name: "\u4E2D\u6587" }
  ];
}

// src/settings.ts
var DEFAULT_SETTINGS = {
  attachmentsPaths: [],
  trashFolderOverride: ".vault-trash",
  ignorePatterns: [],
  alternativeAttachmentAlg: false,
  deleteStrategy: "custom-folder",
  protectedDays: 7,
  whitelistFolders: [],
  autoCleanEnabled: false,
  autoCleanIntervalDays: 3,
  autoCleanOnVaultOpen: true,
  autoCleanOnPluginLoad: true,
  language: "en-US"
};
var CSS_CLASS_CHECK_PASS = "vault-cleaner-pass";
var CSS_CLASS_CHECK_FAIL = "vault-cleaner-fail";
var VaultCleanerSettingsTab = class extends import_obsidian.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
  }
  display() {
    const { containerEl } = this;
    containerEl.empty();
    setLanguage(this.plugin.settings.language);
    const trans = t();
    containerEl.createEl("h3", {
      attr: { "style": "text-align: center;" },
      text: trans.settings.title
    });
    this.renderLanguageSetting(containerEl);
    this.renderCleanupSettings(containerEl);
    this.renderAutoCleanSettings(containerEl);
    this.renderAdvancedSettings(containerEl);
  }
  renderLanguageSetting(containerEl) {
    containerEl.createEl("h3", { text: "\u{1F310} Language / \u8BED\u8A00" });
    new import_obsidian.Setting(containerEl).setName("Interface Language").setDesc("Choose your preferred language").addDropdown((dropdown) => {
      getAvailableLanguages().forEach((lang) => {
        dropdown.addOption(lang.code, lang.name);
      });
      dropdown.setValue(this.plugin.settings.language);
      dropdown.onChange((value) => __async(this, null, function* () {
        this.plugin.settings.language = value;
        yield this.plugin.saveSettings();
        setLanguage(value);
        this.display();
      }));
    });
  }
  renderCleanupSettings(containerEl) {
    const trans = t();
    containerEl.createEl("h3", { text: trans.settings.cleanupSettings });
    new import_obsidian.Setting(containerEl).setName(trans.settings.deleteStrategy).setDesc(trans.settings.deleteStrategyDesc).addDropdown((dropdown) => {
      dropdown.addOption("custom-folder", trans.settings.strategyCustom);
      dropdown.addOption("permanent", trans.settings.strategyPermanent);
      dropdown.setValue(this.plugin.settings.deleteStrategy);
      dropdown.onChange((value) => __async(this, null, function* () {
        this.plugin.settings.deleteStrategy = value;
        yield this.plugin.saveSettings();
        this.display();
      }));
    });
    new import_obsidian.Setting(containerEl).setName(trans.settings.customTrashFolder).setDesc(trans.settings.customTrashFolderDesc).addText((text) => text.setPlaceholder(".vault-trash").setValue(this.plugin.settings.trashFolderOverride).onChange((value) => __async(this, null, function* () {
      this.plugin.settings.trashFolderOverride = value;
      yield this.plugin.saveSettings();
    })));
    new import_obsidian.Setting(containerEl).setName(trans.settings.protectedDays).setDesc(trans.settings.protectedDaysDesc).addText((text) => {
      text.inputEl.type = "number";
      text.setPlaceholder("7").setValue(String(this.plugin.settings.protectedDays)).onChange((value) => __async(this, null, function* () {
        const num = parseInt(value, 10);
        if (!isNaN(num) && num >= 0) {
          this.plugin.settings.protectedDays = num;
          yield this.plugin.saveSettings();
        }
      }));
    });
    new import_obsidian.Setting(containerEl).setName(trans.settings.whitelistFolders).setDesc(trans.settings.whitelistFoldersDesc).addTextArea((text) => text.setPlaceholder("attachments/important\ndocs/archive").setValue(this.plugin.settings.whitelistFolders.join("\n")).onChange((value) => __async(this, null, function* () {
      this.plugin.settings.whitelistFolders = value.split("\n").map((x) => x.trim()).filter((x) => x.length > 0);
      yield this.plugin.saveSettings();
    })));
  }
  renderAutoCleanSettings(containerEl) {
    const trans = t();
    containerEl.createEl("h3", { text: trans.settings.autoCleanSettings });
    new import_obsidian.Setting(containerEl).setName(trans.settings.enableAutoClean).setDesc(trans.settings.enableAutoCleanDesc).addToggle((toggle) => toggle.setValue(this.plugin.settings.autoCleanEnabled).onChange((value) => __async(this, null, function* () {
      this.plugin.settings.autoCleanEnabled = value;
      yield this.plugin.saveSettings();
      this.display();
    })));
    if (this.plugin.settings.autoCleanEnabled) {
      new import_obsidian.Setting(containerEl).setName(trans.settings.cleanInterval).setDesc(trans.settings.cleanIntervalDesc).addText((text) => {
        text.inputEl.type = "number";
        text.setPlaceholder("3").setValue(String(this.plugin.settings.autoCleanIntervalDays)).onChange((value) => __async(this, null, function* () {
          const num = parseInt(value, 10);
          if (!isNaN(num) && num >= 1) {
            this.plugin.settings.autoCleanIntervalDays = num;
            yield this.plugin.saveSettings();
          }
        }));
      });
      new import_obsidian.Setting(containerEl).setName(trans.settings.checkOnVaultOpen).setDesc(trans.settings.checkOnVaultOpenDesc).addToggle((toggle) => toggle.setValue(this.plugin.settings.autoCleanOnVaultOpen).onChange((value) => __async(this, null, function* () {
        this.plugin.settings.autoCleanOnVaultOpen = value;
        yield this.plugin.saveSettings();
      })));
      new import_obsidian.Setting(containerEl).setName(trans.settings.checkOnPluginLoad).setDesc(trans.settings.checkOnPluginLoadDesc).addToggle((toggle) => toggle.setValue(this.plugin.settings.autoCleanOnPluginLoad).onChange((value) => __async(this, null, function* () {
        this.plugin.settings.autoCleanOnPluginLoad = value;
        yield this.plugin.saveSettings();
      })));
    }
  }
  renderAdvancedSettings(containerEl) {
    const trans = t();
    containerEl.createEl("h3", { text: trans.settings.advancedSettings });
    new import_obsidian.Setting(containerEl).setName(trans.settings.attachmentFolder).setDesc(trans.settings.attachmentFolderDesc).addTextArea((text) => text.setPlaceholder(this.app.vault.config.attachmentFolderPath).setValue(this.plugin.settings.attachmentsPaths.join("\n")).onChange((value) => __async(this, null, function* () {
      this.plugin.settings.attachmentsPaths = value.split("\n").map((x) => x.trim()).filter((x) => x.length > 0);
      yield this.plugin.saveSettings();
    })));
    new import_obsidian.Setting(containerEl).setName(trans.settings.ignorePatterns).setDesc(trans.settings.ignorePatternsDesc).addTextArea((text) => text.setValue(this.plugin.settings.ignorePatterns.join("\n")).onChange((value) => __async(this, null, function* () {
      this.plugin.settings.ignorePatterns = value.split("\n").map((x) => x.trim()).filter((x) => x.length > 0);
      yield this.plugin.saveSettings();
    })));
    new import_obsidian.Setting(containerEl).setName(trans.settings.testSettings).setDesc(trans.settings.testSettingsDesc).addText((text) => {
      const resetColor = () => {
        text.inputEl.classList.remove(CSS_CLASS_CHECK_PASS, CSS_CLASS_CHECK_FAIL);
      };
      text.onChange((value) => {
        resetColor();
        if (value.length === 0)
          return;
        if (this.plugin.getIgnoreFilter().test(value))
          text.inputEl.classList.add(CSS_CLASS_CHECK_FAIL);
        else
          text.inputEl.classList.add(CSS_CLASS_CHECK_PASS);
      });
      text.inputEl.addEventListener("focusout", () => resetColor());
      text.inputEl.addEventListener("focusin", () => text.onChanged());
    });
    new import_obsidian.Setting(containerEl).setName(trans.settings.alternativeAlg).setDesc(trans.settings.alternativeAlgDesc).addToggle((btn) => btn.setValue(this.plugin.settings.alternativeAttachmentAlg).onChange((value) => __async(this, null, function* () {
      this.plugin.settings.alternativeAttachmentAlg = value;
      yield this.plugin.saveSettings();
    })));
  }
};

// src/trash_modal.ts
var import_obsidian2 = __toModule(require("obsidian"));
var path = __toModule(require("path"));
var TrashFilesModal = class extends import_obsidian2.Modal {
  constructor(app, files, trashFolderPath, useSystemTrash) {
    super(app);
    this.files = files;
    this.trashFolderPath = trashFolderPath;
    this.useSystemTrash = useSystemTrash;
  }
  onOpen() {
    let { contentEl, titleEl } = this;
    titleEl.setText("Move " + this.files.length + " files to trash?");
    const div = contentEl.createDiv({
      cls: "trash-modal-file-links"
    });
    this.files.forEach((file) => {
      div.createEl("p", {
        cls: "trash-modal-link",
        text: file.path
      }).addEventListener("click", () => __async(this, null, function* () {
        this.close();
        yield this.app.workspace.activeLeaf.openFile(file);
      }));
    });
    contentEl.createEl("button", {
      cls: ["trash-modal-button"],
      text: "Cancel"
    }).addEventListener("click", () => this.close());
    contentEl.createEl("button", {
      cls: ["trash-modal-button"],
      text: "Copy list to clipboard"
    }).addEventListener("click", () => __async(this, null, function* () {
      yield navigator.clipboard.writeText(this.files.map((file) => file.path).join("\n"));
      new import_obsidian2.Notice("Copied list to clipboard");
    }));
    contentEl.createEl("button", {
      cls: ["mod-cta", "trash-modal-button"],
      text: "Trash"
    }).addEventListener("click", () => __async(this, null, function* () {
      if (this.trashFolderPath.length > 0) {
        if (!(yield this.app.vault.adapter.exists(this.trashFolderPath)))
          yield this.app.vault.createFolder(this.trashFolderPath);
        this.files.forEach((file) => __async(this, null, function* () {
          return yield this.app.fileManager.renameFile(file, path.join(this.trashFolderPath, file.name));
        }));
      } else
        this.files.forEach((file) => __async(this, null, function* () {
          return yield this.app.vault.trash(file, this.useSystemTrash);
        }));
      new import_obsidian2.Notice("Trashed " + this.files.length + " files");
      this.close();
    }));
  }
  onClose() {
    let { contentEl } = this;
    contentEl.empty();
  }
};

// src/scanService.ts
var CustomFilter = class {
  constructor(regexes, strings) {
    this.regexes = new Set(regexes.map((x) => RegExp(x)));
    this.strings = new Set(strings);
  }
  test(input) {
    return Array.from(this.regexes).some((x) => x.test(input)) || Array.from(this.strings).some((x) => x === input);
  }
};
var ScanService = class {
  constructor(plugin) {
    this.plugin = plugin;
    this.app = plugin.app;
  }
  getIgnoreFilter() {
    const strings = [];
    if (this.plugin.settings.trashFolderOverride.length > 0) {
      strings.push(this.plugin.settings.trashFolderOverride);
    }
    this.plugin.settings.whitelistFolders.forEach((folder) => {
      if (folder.length > 0) {
        strings.push(folder);
      }
    });
    return new CustomFilter(this.plugin.settings.ignorePatterns, strings);
  }
  getAttachmentsPaths() {
    if (this.plugin.settings.attachmentsPaths.length === 0) {
      return [this.app.vault.config.attachmentFolderPath];
    }
    return this.plugin.settings.attachmentsPaths;
  }
  isAttachment(file) {
    return this.getAttachmentsPaths().some((element) => {
      if (element.startsWith("./")) {
        if (this.plugin.settings.alternativeAttachmentAlg) {
          let path3 = file.parent;
          while (path3 && path3.name !== void 0 && path3.name.length > 0) {
            if (path3.name === element.substring(2)) {
              return true;
            }
            path3 = path3.parent;
          }
        } else {
          return file.path.startsWith(element.substring(2)) || file.path.contains(element.substring(1) + "/");
        }
      } else {
        if (file.parent && file.parent.path === element) {
          return true;
        }
        if (file.path.startsWith(element)) {
          return true;
        }
      }
      return false;
    });
  }
  getCanvasLinks() {
    return __async(this, null, function* () {
      const links = new Set();
      yield Promise.all(this.app.vault.getFiles().filter((f) => f.extension === "canvas").map((f) => __async(this, null, function* () {
        const content = yield this.app.vault.read(f);
        try {
          const canvas = JSON.parse(content);
          canvas.nodes.filter((node) => node.type === "file").forEach((node) => links.add(node.file));
        } catch (e) {
          console.error("Error parsing canvas file " + f.path + "\n", e);
        }
      })));
      return links;
    });
  }
  isProtected(file) {
    if (this.plugin.settings.protectedDays <= 0) {
      return false;
    }
    const now = Date.now();
    const protectedMs = this.plugin.settings.protectedDays * 24 * 60 * 60 * 1e3;
    const fileModifiedTime = file.stat.mtime;
    return now - fileModifiedTime < protectedMs;
  }
  scanVault() {
    return __async(this, null, function* () {
      const links = new Set(Object.values(this.app.metadataCache.resolvedLinks).flatMap((x) => Object.keys(x)));
      const canvasLinks = yield this.getCanvasLinks();
      const filter = this.getIgnoreFilter();
      const orphans = this.app.vault.getFiles().filter((file) => {
        return ![
          links.has(file.path),
          canvasLinks.has(file.path),
          filter.test(file.path),
          this.isProtected(file)
        ].some((x) => x === true);
      });
      const orphanAttachments = orphans.filter((file) => this.isAttachment(file));
      const orphanNotes = orphans.filter((file) => file.extension === "md");
      return {
        orphans,
        orphanAttachments,
        orphanNotes
      };
    });
  }
};

// src/actionService.ts
var import_obsidian3 = __toModule(require("obsidian"));
var path2 = __toModule(require("path"));
var ActionService = class {
  constructor(plugin) {
    this.plugin = plugin;
    this.app = plugin.app;
  }
  formatFileSize(bytes) {
    if (bytes < 1024) {
      return bytes + " B";
    } else if (bytes < 1024 * 1024) {
      return (bytes / 1024).toFixed(1) + " KB";
    } else {
      return (bytes / (1024 * 1024)).toFixed(1) + " MB";
    }
  }
  getFileType(file) {
    setLanguage(this.plugin.settings.language);
    const trans = t();
    const ext = file.extension.toLowerCase();
    const typeMap = {
      "md": trans.preview.fileType === "\u7C7B\u578B" ? "\u7B14\u8BB0" : "Note",
      "canvas": trans.preview.fileType === "\u7C7B\u578B" ? "\u753B\u5E03" : "Canvas",
      "png": trans.preview.fileType === "\u7C7B\u578B" ? "\u56FE\u7247" : "Image",
      "jpg": trans.preview.fileType === "\u7C7B\u578B" ? "\u56FE\u7247" : "Image",
      "jpeg": trans.preview.fileType === "\u7C7B\u578B" ? "\u56FE\u7247" : "Image",
      "gif": trans.preview.fileType === "\u7C7B\u578B" ? "\u56FE\u7247" : "Image",
      "webp": trans.preview.fileType === "\u7C7B\u578B" ? "\u56FE\u7247" : "Image",
      "svg": trans.preview.fileType === "\u7C7B\u578B" ? "\u56FE\u7247" : "Image",
      "pdf": "PDF",
      "mp4": trans.preview.fileType === "\u7C7B\u578B" ? "\u89C6\u9891" : "Video",
      "webm": trans.preview.fileType === "\u7C7B\u578B" ? "\u89C6\u9891" : "Video",
      "mp3": trans.preview.fileType === "\u7C7B\u578B" ? "\u97F3\u9891" : "Audio",
      "wav": trans.preview.fileType === "\u7C7B\u578B" ? "\u97F3\u9891" : "Audio",
      "ogg": trans.preview.fileType === "\u7C7B\u578B" ? "\u97F3\u9891" : "Audio",
      "txt": trans.preview.fileType === "\u7C7B\u578B" ? "\u6587\u672C" : "Text",
      "csv": trans.preview.fileType === "\u7C7B\u578B" ? "\u8868\u683C" : "Spreadsheet",
      "xlsx": trans.preview.fileType === "\u7C7B\u578B" ? "\u8868\u683C" : "Spreadsheet",
      "docx": trans.preview.fileType === "\u7C7B\u578B" ? "\u6587\u6863" : "Document"
    };
    return typeMap[ext] || ext.toUpperCase();
  }
  prepareFileList(files) {
    setLanguage(this.plugin.settings.language);
    const trans = t();
    return files.map((file) => ({
      file,
      type: this.getFileType(file),
      size: this.formatFileSize(file.stat.size),
      status: trans.preview.unreferenced
    }));
  }
  executeDelete(files) {
    return __async(this, null, function* () {
      setLanguage(this.plugin.settings.language);
      const trans = t();
      if (files.length === 0) {
        new import_obsidian3.Notice(trans.action.noFilesToDelete);
        return;
      }
      const strategy = this.plugin.settings.deleteStrategy;
      try {
        switch (strategy) {
          case "custom-folder":
            yield this.moveToCustomTrash(files);
            break;
          case "permanent":
            yield this.permanentDelete(files);
            break;
        }
        new import_obsidian3.Notice(trans.action.deleteSuccess.replace("{count}", String(files.length)));
      } catch (error) {
        console.error("Error during file deletion:", error);
        new import_obsidian3.Notice(trans.action.deleteFailed.replace("{error}", error.message));
      }
    });
  }
  moveToCustomTrash(files) {
    return __async(this, null, function* () {
      const trashPath = this.plugin.settings.trashFolderOverride || ".vault-trash";
      if (!(yield this.app.vault.adapter.exists(trashPath))) {
        yield this.app.vault.createFolder(trashPath);
      }
      for (const file of files) {
        const destPath = path2.posix.join(trashPath, file.name);
        yield this.app.fileManager.renameFile(file, destPath);
      }
    });
  }
  permanentDelete(files) {
    return __async(this, null, function* () {
      for (const file of files) {
        yield this.app.vault.delete(file, true);
      }
    });
  }
  undoDelete(files) {
    return __async(this, null, function* () {
      setLanguage(this.plugin.settings.language);
      const trans = t();
      const strategy = this.plugin.settings.deleteStrategy;
      if (strategy === "permanent") {
        new import_obsidian3.Notice(trans.action.cannotUndo);
        return false;
      }
      if (strategy === "custom-folder") {
        const trashPath = this.plugin.settings.trashFolderOverride || ".vault-trash";
        try {
          for (const file of files) {
            const trashFilePath = path2.posix.join(trashPath, file.name);
            if (yield this.app.vault.adapter.exists(trashFilePath)) {
              const trashFile = this.app.vault.getAbstractFileByPath(trashFilePath);
              if (trashFile instanceof import_obsidian3.TFile) {
                const originalPath = file.path;
                yield this.app.fileManager.renameFile(trashFile, originalPath);
              }
            }
          }
          new import_obsidian3.Notice(trans.action.restoreSuccess.replace("{count}", String(files.length)));
          return true;
        } catch (error) {
          console.error("Error during undo:", error);
          new import_obsidian3.Notice(trans.action.restoreFailed);
          return false;
        }
      }
      new import_obsidian3.Notice(trans.action.undoNotSupported);
      return false;
    });
  }
};

// src/previewModal.ts
var import_obsidian4 = __toModule(require("obsidian"));
var PreviewModal = class extends import_obsidian4.Modal {
  constructor(app, files, onConfirm, language) {
    super(app);
    this.confirmed = false;
    this.files = files;
    this.onConfirm = onConfirm;
    this.language = language;
  }
  onOpen() {
    setLanguage(this.language);
    const trans = t();
    const { contentEl, titleEl } = this;
    contentEl.empty();
    titleEl.setText(trans.preview.title.replace("{count}", String(this.files.length)));
    const container = contentEl.createDiv({ cls: "preview-modal-container" });
    const fileList = container.createDiv({ cls: "preview-file-list" });
    this.files.forEach((info) => {
      const fileItem = fileList.createDiv({ cls: "preview-file-item" });
      const icon = info.file.extension === "md" ? "\u{1F4DD}" : "\u{1F4CE}";
      fileItem.createEl("div", {
        cls: "preview-file-icon",
        text: icon
      });
      const fileInfo = fileItem.createDiv({ cls: "preview-file-info" });
      fileInfo.createEl("div", {
        cls: "preview-file-name",
        text: info.file.name
      });
      fileInfo.createEl("div", {
        cls: "preview-file-path",
        text: `${trans.preview.filePath}: ${info.file.path}`
      });
      fileInfo.createEl("div", {
        cls: "preview-file-meta",
        text: `${trans.preview.fileSize}: ${info.size} | ${trans.preview.fileType}: ${info.type} | ${trans.preview.fileStatus}: ${info.status}`
      });
    });
    const warningBox = container.createDiv({ cls: "preview-warning" });
    warningBox.createEl("p", {
      text: trans.preview.warning
    });
    const checkboxContainer = container.createDiv({ cls: "preview-checkbox-container" });
    const checkbox = checkboxContainer.createEl("input", {
      type: "checkbox",
      cls: "preview-confirm-checkbox"
    });
    checkboxContainer.createEl("label", {
      cls: "preview-checkbox-label",
      text: trans.preview.confirmCheckbox
    });
    const buttonContainer = container.createDiv({ cls: "preview-button-container" });
    buttonContainer.createEl("button", {
      cls: ["mod-cancel", "preview-button"],
      text: trans.preview.cancel
    }).addEventListener("click", () => this.close());
    const confirmButton = buttonContainer.createEl("button", {
      cls: ["mod-cta", "preview-button"],
      text: trans.preview.execute
    });
    confirmButton.disabled = true;
    checkbox.addEventListener("change", () => {
      confirmButton.disabled = !checkbox.checked;
    });
    confirmButton.addEventListener("click", () => {
      if (checkbox.checked) {
        this.confirmed = true;
        this.onConfirm();
        this.close();
      }
    });
  }
  onClose() {
    const { contentEl } = this;
    contentEl.empty();
  }
};

// src/dashboardModal.ts
var import_obsidian5 = __toModule(require("obsidian"));
var DashboardModal = class extends import_obsidian5.Modal {
  constructor(app, plugin) {
    super(app);
    this.scanResult = null;
    this.isScanning = false;
    this.plugin = plugin;
    this.scanService = new ScanService(plugin);
    this.actionService = new ActionService(plugin);
  }
  onOpen() {
    setLanguage(this.plugin.settings.language);
    const trans = t();
    const { contentEl, titleEl } = this;
    contentEl.empty();
    titleEl.setText(trans.dashboard.title);
    const container = contentEl.createDiv({ cls: "dashboard-container" });
    this.renderScanStatus(container);
    this.renderStats(container);
    this.renderActions(container);
    this.renderAutoCleanStatus(container);
    this.renderSafetyNotice(container);
  }
  renderScanStatus(container) {
    const trans = t();
    const statusSection = container.createDiv({ cls: "dashboard-section" });
    statusSection.createEl("h3", { text: trans.dashboard.scanStatus });
    const statusEl = statusSection.createDiv({ cls: "scan-status" });
    if (this.isScanning) {
      statusEl.createEl("p", { text: trans.dashboard.scanning });
    } else if (this.scanResult) {
      statusEl.createEl("p", { text: trans.dashboard.scanComplete });
    } else {
      statusEl.createEl("p", { text: trans.dashboard.notScanned });
    }
  }
  renderStats(container) {
    const trans = t();
    const statsSection = container.createDiv({ cls: "dashboard-section" });
    statsSection.createEl("h3", { text: trans.dashboard.stats });
    const statsEl = statsSection.createDiv({ cls: "stats-container" });
    if (this.scanResult) {
      statsEl.createEl("div", {
        cls: "stat-item",
        text: `${trans.dashboard.orphanAttachments}: ${this.scanResult.orphanAttachments.length}`
      });
      statsEl.createEl("div", {
        cls: "stat-item",
        text: `${trans.dashboard.orphanNotes}: ${this.scanResult.orphanNotes.length}`
      });
      statsEl.createEl("div", {
        cls: "stat-item",
        text: `${trans.dashboard.totalOrphans}: ${this.scanResult.orphans.length}`
      });
    } else {
      statsEl.createEl("div", {
        cls: "stat-item",
        text: `${trans.dashboard.orphanAttachments}: --`
      });
      statsEl.createEl("div", {
        cls: "stat-item",
        text: `${trans.dashboard.orphanNotes}: --`
      });
      statsEl.createEl("div", {
        cls: "stat-item",
        text: `${trans.dashboard.totalOrphans}: --`
      });
    }
  }
  renderActions(container) {
    const trans = t();
    const actionsSection = container.createDiv({ cls: "dashboard-section" });
    actionsSection.createEl("h3", { text: trans.dashboard.actions });
    const actionsEl = actionsSection.createDiv({ cls: "actions-container" });
    const scanButton = actionsEl.createEl("button", {
      cls: ["mod-cta", "dashboard-button", "scan-button"],
      text: trans.dashboard.scanVault
    });
    scanButton.addEventListener("click", () => __async(this, null, function* () {
      yield this.performScan();
    }));
    const cleanAttachmentsBtn = actionsEl.createEl("button", {
      cls: ["dashboard-button"],
      text: trans.dashboard.cleanAttachmentsBtn
    });
    cleanAttachmentsBtn.addEventListener("click", () => {
      this.cleanAttachments();
    });
    const cleanNotesBtn = actionsEl.createEl("button", {
      cls: ["dashboard-button"],
      text: trans.dashboard.cleanNotesBtn
    });
    cleanNotesBtn.addEventListener("click", () => {
      this.cleanNotes();
    });
    const cleanAllBtn = actionsEl.createEl("button", {
      cls: ["dashboard-button", "danger-button"],
      text: trans.dashboard.cleanAllBtn
    });
    cleanAllBtn.addEventListener("click", () => {
      this.cleanAll();
    });
  }
  renderAutoCleanStatus(container) {
    const trans = t();
    const autoSection = container.createDiv({ cls: "dashboard-section" });
    autoSection.createEl("h3", { text: trans.dashboard.autoCleanStatus });
    const autoEl = autoSection.createDiv({ cls: "auto-clean-status" });
    if (this.plugin.settings.autoCleanEnabled) {
      autoEl.createEl("p", { text: trans.dashboard.autoCleanEnabled });
      autoEl.createEl("p", {
        text: trans.dashboard.everyDays.replace("{days}", String(this.plugin.settings.autoCleanIntervalDays))
      });
    } else {
      autoEl.createEl("p", { text: trans.dashboard.autoCleanDisabled });
    }
    let strategyText = trans.dashboard.strategyCustom;
    if (this.plugin.settings.deleteStrategy === "permanent") {
      strategyText = trans.dashboard.strategyPermanent;
    }
    autoEl.createEl("p", { text: `${trans.dashboard.deleteStrategy}: ${strategyText}` });
  }
  renderSafetyNotice(container) {
    const trans = t();
    const safetySection = container.createDiv({ cls: "dashboard-section" });
    safetySection.createEl("h3", { text: trans.dashboard.safetyNotice });
    const safetyEl = safetySection.createDiv({ cls: "safety-notice" });
    safetyEl.createEl("p", {
      text: trans.dashboard.safetyProtected.replace("{days}", String(this.plugin.settings.protectedDays))
    });
    safetyEl.createEl("p", { text: trans.dashboard.safetyPreview });
    safetyEl.createEl("p", { text: trans.dashboard.safetyAutoClean });
  }
  performScan() {
    return __async(this, null, function* () {
      const trans = t();
      this.isScanning = true;
      new import_obsidian5.Notice(trans.dashboard.scanVault);
      this.onOpen();
      try {
        this.scanResult = yield this.scanService.scanVault();
        new import_obsidian5.Notice(trans.dashboard.scanCompleteNotice.replace("{count}", String(this.scanResult.orphans.length)));
      } catch (error) {
        new import_obsidian5.Notice(trans.dashboard.scanFailed.replace("{error}", error.message));
        console.error("Scan error:", error);
      }
      this.isScanning = false;
      this.onOpen();
    });
  }
  cleanAttachments() {
    const trans = t();
    if (!this.scanResult) {
      new import_obsidian5.Notice(trans.dashboard.scanFirst);
      return;
    }
    if (this.scanResult.orphanAttachments.length === 0) {
      new import_obsidian5.Notice(trans.plugin.noOrphanAttachments);
      return;
    }
    const fileInfos = this.actionService.prepareFileList(this.scanResult.orphanAttachments);
    new PreviewModal(this.app, fileInfos, () => __async(this, null, function* () {
      yield this.actionService.executeDelete(this.scanResult.orphanAttachments);
      this.scanResult = yield this.scanService.scanVault();
      this.onOpen();
    }), this.plugin.settings.language).open();
  }
  cleanNotes() {
    const trans = t();
    if (!this.scanResult) {
      new import_obsidian5.Notice(trans.dashboard.scanFirst);
      return;
    }
    if (this.scanResult.orphanNotes.length === 0) {
      new import_obsidian5.Notice(trans.plugin.noOrphanNotes);
      return;
    }
    const fileInfos = this.actionService.prepareFileList(this.scanResult.orphanNotes);
    new PreviewModal(this.app, fileInfos, () => __async(this, null, function* () {
      yield this.actionService.executeDelete(this.scanResult.orphanNotes);
      this.scanResult = yield this.scanService.scanVault();
      this.onOpen();
    }), this.plugin.settings.language).open();
  }
  cleanAll() {
    const trans = t();
    if (!this.scanResult) {
      new import_obsidian5.Notice(trans.dashboard.scanFirst);
      return;
    }
    if (this.scanResult.orphans.length === 0) {
      new import_obsidian5.Notice(trans.plugin.noOrphans);
      return;
    }
    const fileInfos = this.actionService.prepareFileList(this.scanResult.orphans);
    new PreviewModal(this.app, fileInfos, () => __async(this, null, function* () {
      yield this.actionService.executeDelete(this.scanResult.orphans);
      this.scanResult = yield this.scanService.scanVault();
      this.onOpen();
    }), this.plugin.settings.language).open();
  }
  onClose() {
    const { contentEl } = this;
    contentEl.empty();
  }
};

// src/autoCleanScheduler.ts
var import_obsidian6 = __toModule(require("obsidian"));
var STORAGE_KEY_LAST_CLEAN = "nuke-cleaner-last-auto-clean";
var AutoCleanScheduler = class {
  constructor(plugin) {
    this.intervalId = null;
    this.plugin = plugin;
    this.scanService = new ScanService(plugin);
    this.actionService = new ActionService(plugin);
  }
  initialize() {
    return __async(this, null, function* () {
      if (!this.plugin.settings.autoCleanEnabled) {
        return;
      }
      if (this.plugin.settings.autoCleanOnPluginLoad) {
        yield this.checkAndRun();
      }
      this.setupInterval();
    });
  }
  onVaultOpen() {
    return __async(this, null, function* () {
      if (!this.plugin.settings.autoCleanEnabled) {
        return;
      }
      if (this.plugin.settings.autoCleanOnVaultOpen) {
        yield this.checkAndRun();
      }
    });
  }
  setupInterval() {
    if (this.intervalId !== null) {
      window.clearInterval(this.intervalId);
    }
    const intervalMs = this.plugin.settings.autoCleanIntervalDays * 24 * 60 * 60 * 1e3;
    this.intervalId = window.setInterval(() => __async(this, null, function* () {
      yield this.checkAndRun();
    }), intervalMs);
  }
  checkAndRun() {
    return __async(this, null, function* () {
      const lastClean = yield this.getLastCleanTime();
      const now = Date.now();
      const intervalMs = this.plugin.settings.autoCleanIntervalDays * 24 * 60 * 60 * 1e3;
      if (lastClean && now - lastClean < intervalMs) {
        return;
      }
      yield this.runAutoClean();
    });
  }
  runAutoClean() {
    return __async(this, null, function* () {
      setLanguage(this.plugin.settings.language);
      const trans = t();
      try {
        const scanResult = yield this.scanService.scanVault();
        if (scanResult.orphanAttachments.length === 0) {
          return;
        }
        yield this.actionService.executeDelete(scanResult.orphanAttachments);
        yield this.setLastCleanTime(Date.now());
        new import_obsidian6.Notice(trans.action.autoCleanNotice.replace("{count}", String(scanResult.orphanAttachments.length)));
      } catch (error) {
        console.error("Auto clean failed:", error);
        new import_obsidian6.Notice(trans.action.autoCleanFailed.replace("{error}", error.message));
      }
    });
  }
  getLastCleanTime() {
    return __async(this, null, function* () {
      try {
        const data = yield this.plugin.loadData();
        if (data && data[STORAGE_KEY_LAST_CLEAN]) {
          return data[STORAGE_KEY_LAST_CLEAN];
        }
      } catch (error) {
        console.error("Failed to load last clean time:", error);
      }
      return null;
    });
  }
  setLastCleanTime(timestamp) {
    return __async(this, null, function* () {
      try {
        const data = (yield this.plugin.loadData()) || {};
        data[STORAGE_KEY_LAST_CLEAN] = timestamp;
        yield this.plugin.saveData(data);
      } catch (error) {
        console.error("Failed to save last clean time:", error);
      }
    });
  }
  destroy() {
    if (this.intervalId !== null) {
      window.clearInterval(this.intervalId);
      this.intervalId = null;
    }
  }
  updateSettings() {
    this.destroy();
    if (this.plugin.settings.autoCleanEnabled) {
      this.setupInterval();
    }
  }
};

// src/main.ts
var CustomFilter2 = class {
  constructor(regexes, strings) {
    this.regexes = new Set(regexes.map((x) => RegExp(x)));
    this.strings = new Set(strings);
  }
  test(input) {
    return Array.from(this.regexes).some((x) => x.test(input)) || Array.from(this.strings).some((x) => x === input);
  }
};
var VaultCleanerPlugin = class extends import_obsidian7.Plugin {
  onload() {
    return __async(this, null, function* () {
      yield this.loadSettings();
      setLanguage(this.settings.language);
      this.scanService = new ScanService(this);
      this.actionService = new ActionService(this);
      this.autoCleanScheduler = new AutoCleanScheduler(this);
      const trans = t();
      this.addRibbonIcon("brush-cleaning", trans.plugin.name, () => {
        new DashboardModal(this.app, this).open();
      });
      this.addCommand({
        id: "open-dashboard",
        name: trans.plugin.openDashboard,
        callback: () => {
          new DashboardModal(this.app, this).open();
        }
      });
      this.addCommand({
        id: "clean-orphaned-attachments",
        name: trans.plugin.cleanAttachments,
        callback: () => __async(this, null, function* () {
          setLanguage(this.settings.language);
          const trans2 = t();
          new import_obsidian7.Notice(trans2.plugin.gatheringAttachments);
          const scanResult = yield this.scanService.scanVault();
          if (scanResult.orphanAttachments.length > 0) {
            const fileInfos = this.actionService.prepareFileList(scanResult.orphanAttachments);
            new PreviewModal(this.app, fileInfos, () => __async(this, null, function* () {
              yield this.actionService.executeDelete(scanResult.orphanAttachments);
            }), this.settings.language).open();
          } else {
            new import_obsidian7.Notice(trans2.plugin.noOrphanAttachments);
          }
        })
      });
      this.addCommand({
        id: "clean-orphaned-notes",
        name: trans.plugin.cleanNotes,
        callback: () => __async(this, null, function* () {
          setLanguage(this.settings.language);
          const trans2 = t();
          new import_obsidian7.Notice(trans2.plugin.gatheringNotes);
          const scanResult = yield this.scanService.scanVault();
          if (scanResult.orphanNotes.length > 0) {
            const fileInfos = this.actionService.prepareFileList(scanResult.orphanNotes);
            new PreviewModal(this.app, fileInfos, () => __async(this, null, function* () {
              yield this.actionService.executeDelete(scanResult.orphanNotes);
            }), this.settings.language).open();
          } else {
            new import_obsidian7.Notice(trans2.plugin.noOrphanNotes);
          }
        })
      });
      this.addCommand({
        id: "clean-orphaned-files",
        name: trans.plugin.cleanAll,
        callback: () => __async(this, null, function* () {
          setLanguage(this.settings.language);
          const trans2 = t();
          new import_obsidian7.Notice(trans2.plugin.gatheringFiles);
          const scanResult = yield this.scanService.scanVault();
          if (scanResult.orphans.length > 0) {
            const fileInfos = this.actionService.prepareFileList(scanResult.orphans);
            new PreviewModal(this.app, fileInfos, () => __async(this, null, function* () {
              yield this.actionService.executeDelete(scanResult.orphans);
            }), this.settings.language).open();
          } else {
            new import_obsidian7.Notice(trans2.plugin.noOrphans);
          }
        })
      });
      this.addSettingTab(new VaultCleanerSettingsTab(this.app, this));
      yield this.autoCleanScheduler.initialize();
      this.registerEvent(this.app.workspace.on("workspace:layout-change", () => __async(this, null, function* () {
        yield this.autoCleanScheduler.onVaultOpen();
      })));
    });
  }
  onunload() {
    this.autoCleanScheduler.destroy();
  }
  loadSettings() {
    return __async(this, null, function* () {
      this.settings = Object.assign({}, DEFAULT_SETTINGS, yield this.loadData());
    });
  }
  saveSettings() {
    return __async(this, null, function* () {
      yield this.saveData(this.settings);
      this.autoCleanScheduler.updateSettings();
    });
  }
  getIgnoreFilter() {
    const strings = [];
    if (this.settings.trashFolderOverride.length > 0) {
      strings.push(this.settings.trashFolderOverride);
    }
    return new CustomFilter2(this.settings.ignorePatterns, strings);
  }
  shouldUseSystemTrash() {
    switch (this.app.vault.config.trashOption) {
      case "system":
        return true;
      default:
        return false;
    }
  }
  getAttachmentsPaths() {
    if (this.settings.attachmentsPaths.length === 0) {
      return [this.app.vault.config.attachmentFolderPath];
    }
    return this.settings.attachmentsPaths;
  }
  isAttachment(file) {
    return this.getAttachmentsPaths().some((element) => {
      if (element.startsWith("./")) {
        if (this.settings.alternativeAttachmentAlg) {
          let path3 = file.parent;
          while (path3 && path3.name !== void 0 && path3.name.length > 0) {
            if (path3.name === element.substring(2)) {
              return true;
            }
            path3 = path3.parent;
          }
        } else {
          return file.path.startsWith(element.substring(2)) || file.path.contains(element.substring(1) + "/");
        }
      } else {
        if (file.parent && file.parent.path === element) {
          return true;
        }
        if (file.path.startsWith(element)) {
          return true;
        }
      }
      return false;
    });
  }
  getCanvasLinks() {
    return __async(this, null, function* () {
      const links = new Set();
      yield Promise.all(this.app.vault.getFiles().filter((f) => f.extension === "canvas").map((f) => __async(this, null, function* () {
        const content = yield this.app.vault.read(f);
        try {
          const canvas = JSON.parse(content);
          canvas.nodes.filter((node) => node.type === "file").forEach((node) => links.add(node.file));
        } catch (e) {
          console.error("Error parsing canvas file " + f.path + "\n", e);
        }
      })));
      return links;
    });
  }
  getOrphans() {
    return __async(this, null, function* () {
      const links = new Set(Object.values(this.app.metadataCache.resolvedLinks).flatMap((x) => Object.keys(x)));
      const canvasLinks = yield this.getCanvasLinks();
      const filter = this.getIgnoreFilter();
      return this.app.vault.getFiles().filter((file) => {
        return ![
          links.has(file.path),
          canvasLinks.has(file.path),
          filter.test(file.path)
        ].some((x) => x === true);
      });
    });
  }
  trash(files) {
    setLanguage(this.settings.language);
    const trans = t();
    if (files.length > 0) {
      new TrashFilesModal(this.app, files, this.settings.trashFolderOverride, this.shouldUseSystemTrash()).open();
    } else {
      new import_obsidian7.Notice(trans.plugin.noOrphans);
    }
  }
};
